import React from 'react';
import styles from 'ErrorsList.module.css';

const ErrorsList = () => {
  return (
    <section>
      {/* {errors.map((e: IError, i: number) => (
        <p onClick={() => removeError(e.id)} key={i}>
          {e.text} {e.id}
        </p>
      ))}
      <button onClick={() => addError("new")}></button> */}
    </section>
  );
};

export default ErrorsList;
